package {
import Long;
public class DivModResult {
	public var quotient: Long;
	public var modulus: Long;

	public function DivModResult(quotient: Long, modulus: Long) {
		this.quotient = quotient;
		this.modulus = modulus;
	}
}
}