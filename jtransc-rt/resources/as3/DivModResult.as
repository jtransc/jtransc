package {
import Int64;
public class DivModResult {
	public var quotient: Int64;
	public var modulus: Int64;

	public function DivModResult(quotient: Int64, modulus: Int64) {
		this.quotient = quotient;
		this.modulus = modulus;
	}
}
}